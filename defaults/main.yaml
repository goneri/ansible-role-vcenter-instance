---
vcenter_instance:
    installation:
        from: iso  # datastore or iso
        iso_path: "{{ lookup('env','HOME') }}/Downloads/VMware-VCSA-all-6.7.0-11726888.iso"
    network:
        name: VM Network
        address: 192.168.123.90
        prefix: 24
        gateway: 192.168.123.1
        dns: ['192.168.123.1']
    appliance:
        hostname: vCenter-Server-Appliance
        name: vCenter-Server-Appliance
        username: 'administrator@vsphere.local'
        password: '!234AaAa56'
    esxi:
        datastore: "{{ datacenter_name }}"
        hostname: "{{ vcenter_hostname }}"
        username: "{{ vcenter_username }}"
        password: "{{ vcenter_password }}"
        datacenter: ha-datacenter
